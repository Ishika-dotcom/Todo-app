# 📝 Todo Web App (Spring Boot + Thymeleaf + JPA + H2)

A simple CRUD web application built with **Spring Boot**, **Spring Data JPA**, **Thymeleaf**, and **Bootstrap**.  
It allows users to **create**, **view**, **edit**, **delete**, and **mark tasks as done** — all through a web interface.

---

## 🚀 Features

- Add new tasks with title, description, and status  
- View all tasks in a styled table  
- Edit or delete existing tasks  
- Mark tasks as **Done** inline  
- Filter tasks by status (All / Pending / Done)  
- Automatically records creation date (`createdAt`)  
- Uses **H2 in-memory database** (no external setup needed)

---

## 🧰 Tech Stack

| Layer | Technology |
|-------|-------------|
| Backend | Spring Boot |
| Database | H2 Database (JPA + Hibernate) |
| Frontend | Thymeleaf Templates |
| Styling | Bootstrap |
| Build Tool | Maven |

---
